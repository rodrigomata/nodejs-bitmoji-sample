module.exports = Avatar => {

    // :: Method that seeds the database with the presets
    // Avatar.generate = async () => {

    // }

    // :: Public method that modifies an avatar and retrieves its info
    Avatar.render = async data => {
        
        console.log('data', data)
        return 
    }
};
